<%- include("partials/header"); -%>

<h1>Home</h1>

<p> <%= info %></p>

<% posts.forEach((post) => { %>
    <h3><%= post.title %></h3>
    <p><%= post.content.substring(0 , 100) + "...." %><a href="/posts/<%= post._id %>" method = "GET">ReadMore</a></p>
<% }) ; %>
    

<%- include("partials/footer"); -%>